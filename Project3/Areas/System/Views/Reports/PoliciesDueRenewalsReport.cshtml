@model IEnumerable<Project3.ModelsView.PoliciesDueRenewalsReportViewModel>
@{
    ViewData["Title"] = "Policies Due for Renewal Report";
    Layout = "~/Areas/System/Views/Layout/LayoutAdmin.cshtml";
}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white text-center">
                    <h2>@ViewData["Title"]</h2>
                </div>
                <div class="card-body">
                    <table class="table table-striped table-bordered">
                        <thead class="thead-dark">
                            <tr>
                                <th>Policy Number</th>
                                <th>Customer Name</th>
                                <th>Vehicle Model</th>
                                <th>Policy End Date</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.PolicyNumber</td>
                                    <td>@item.CustomerName</td>
                                    <td>@item.VehicleModel</td>
                                    <td>@item.PolicyEndDate.ToShortDateString()</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
